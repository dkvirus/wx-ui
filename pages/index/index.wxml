
<view class="container">
  <block wx:for="{{comps}}" wx:key="link">
    <navigator url="{{item.link}}">
      <view class="item">{{index + 1}}. {{item.name}}</view>
    </navigator>
  </block> 
</view>
